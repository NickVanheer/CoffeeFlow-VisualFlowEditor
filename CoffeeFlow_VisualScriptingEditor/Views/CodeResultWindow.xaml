<Window x:Class="CoffeeFlow.Views.CodeResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Debug Output" Height="500" Width="500" DataContext="{Binding Source={StaticResource Locator}}">
    <Grid>
        <ListBox ItemsSource="{Binding Main.DebugList}">
            
        </ListBox>
        <!--
        <TextBox
  Name="tbMultiLine" Text="{Binding Main.Debug}"
  TextWrapping="Wrap"
  AcceptsReturn="True"
  VerticalScrollBarVisibility="Visible"
/>
        !-->
    </Grid>
</Window>
